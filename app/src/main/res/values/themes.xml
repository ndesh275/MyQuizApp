<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyQuizApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/orange</item>
    </style>

    <style name="Theme.MyQuizApp" parent="Base.Theme.MyQuizApp" />

    <style name="splashscreenTheme" parent="Theme.Material3.DayNight">
        <item name="android:windowBackground">@drawable/splash_icon</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MyQuizApp</item>

    </style>
</resources>